import React, { Component, Fragment } from 'react';

class Nav extends Component {
    constructor(props) {
        super(props);
     }

    render() {
        return (
            <Fragment>
             // user icon & name
             // create new layer
             // author layers
             // editor layers
             // viewers layers
            </Fragment>
        );
    }
}

export default Nav;